import { escapeHtml, htmlScriptConfig } from './review-page-utils.mjs';

const storyAuthoringStates = new Set(['RISK_REVIEW', 'AWAITING_STORY_APPROVAL', 'DESIGN_REVIEW']);

export function storyWorkshopPage(changeCase, governance, options) {
  return renderStoryWorkshopPage(changeCase, governance, options);
}

export function storyWorkshopPageWithModelSelector(changeCase, governance, options) {
  return renderStoryWorkshopPage(changeCase, governance, options);
}

function renderStoryWorkshopPage(changeCase, governance, options) {
  const isReady = storyAuthoringStates.has(changeCase.state);
  const canEdit = isReady && options.canAuthor;
  const activeStage = changeCase.state === 'RISK_REVIEW' ? 'draft' : changeCase.state === 'AWAITING_STORY_APPROVAL' ? 'review' : 'design';
  const models = options.aiStatus.models ?? [];
  const templates = options.aiStatus.templates ?? [];
  const modelOptions = models.map((model) => `<option value="${escapeHtml(model)}">${escapeHtml(model)}</option>`).join('');
  const templateOptions = templates.map((template) => `<option value="${escapeHtml(template.id)}">${escapeHtml(template.label)}${template.description ? ` - ${escapeHtml(template.description)}` : ''}</option>`).join('');
  const config = htmlScriptConfig({ storiesEndpoint: options.storiesEndpoint, storySuggestionsEndpoint: options.storySuggestionsEndpoint, expectedVersion: changeCase.projectionVersion, storyReviewUrl: options.storyReviewUrl });
  const authoring = !isReady
    ? `<section class="notice"><p class="eyebrow">NOT READY</p><h2>Story shaping opens after risk classification</h2><p>Complete intake and risk classification before creating the story set.</p></section>`
    : !options.canAuthor
      ? `<section class="notice"><p class="eyebrow">READ ONLY</p><h2>You can view the breakdown, but cannot submit it</h2><p>An authorized contributor must submit a revision for independent Gate B review.</p></section>`
      : `<section class="workspace"><div class="heading"><div><p class="eyebrow">STORY BREAKDOWN</p><h2>Shape a reviewable story set</h2></div><p>Drafts stay editable until you explicitly submit them for independent Gate B review.</p></div>${options.aiStatus.configured && models.length ? `<section class="assistant"><div class="heading"><div><p class="eyebrow">OPTIONAL ASSISTANCE</p><h3>Generate a draft to curate</h3></div><p>Suggestions are never saved or approved automatically.</p></div><div class="pickers"><label>Approved model<select id="story-ai-model">${modelOptions}</select><small>Provided by ${escapeHtml(options.aiStatus.providerLabel ?? 'the configured service')}.</small></label><label>Reviewed Story specification<select id="story-spec-template"><option value="">Retained context only</option>${templateOptions}</select><small>Only reviewed specifications are available.</small></label></div><button class="secondary" id="suggest-stories" type="button">Generate suggestions</button><p id="suggestion-status" class="status" role="status" aria-live="polite"></p><p id="applied-spec" class="applied-spec" role="status" aria-live="polite" hidden></p><div id="suggestion-list" class="suggestion-list" aria-live="polite"></div><div id="suggestion-actions" class="actions" hidden><label><input id="select-all-stories" type="checkbox"> Select all stories</label><button id="accept-suggestions" class="button" type="button" disabled>Accept selected suggestions</button></div></section>` : `<section class="assistant unavailable"><p class="eyebrow">OPTIONAL ASSISTANCE</p><h3>Suggestions are unavailable</h3><p>Manual authoring is available. An administrator can enable an approved server-side model; no browser key is used.</p></section>`}<form id="story-workshop-form" novalidate><div id="story-list" aria-live="polite"></div><template id="story-template"><article class="story-card"><div class="story-head"><strong class="story-number"></strong><button class="remove" type="button">Remove story</button></div><label>Story title<input name="title" required placeholder="Describe the user outcome"></label><label>User need<textarea name="narrative" required placeholder="As a ..., I want ..., so that ..."></textarea></label><fieldset><legend>Acceptance example</legend><label>Given<textarea name="given" required></textarea></label><label>When<textarea name="when" required></textarea></label><label>Then<textarea name="then" required></textarea></label></fieldset></article></template><div class="actions"><button class="secondary" id="add-story" type="button">Add story</button><button class="button" type="submit">Submit stories for independent review</button></div><p id="workshop-status" class="status" role="status" aria-live="polite"></p></form></section>`;
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>ADX Story Breakdown - ${escapeHtml(changeCase.title)}</title><style>${storyWorkshopStyles}</style></head><body><main><header><a class="adx-home-link" href="${escapeHtml(options.homeUrl)}">&larr; Back to ADX home</a><div><p class="eyebrow">CHANGE CASE / STORY BREAKDOWN</p><h1>${escapeHtml(changeCase.title)}</h1></div><ol class="progress" aria-label="Story breakdown stages"><li class="${activeStage === 'draft' ? 'active' : ''}"${activeStage === 'draft' ? ' aria-current="step"' : ''}>Draft</li><li class="${activeStage === 'review' ? 'active' : ''}"${activeStage === 'review' ? ' aria-current="step"' : ''}>Review</li><li class="${activeStage === 'design' ? 'active' : ''}"${activeStage === 'design' ? ' aria-current="step"' : ''}>Design</li></ol><p class="meta">${escapeHtml(changeCase.riskTier)} risk / ${escapeHtml(changeCase.state)} / Version ${escapeHtml(changeCase.projectionVersion)}</p></header><section class="context"><p class="eyebrow">CURRENT STEP</p><h2>Draft stories, then send them to review</h2><p>Suggestions are a preview only. Nothing is persisted or approved until you submit the edited story set.</p></section>${authoring}</main>${canEdit ? `<script>${storyWorkshopController(config)}</script>` : ''}</body></html>`;
}

const storyWorkshopStyles = `:root{background:#f3f6f4;color:#17332f;font:16px/1.5 Georgia,"Times New Roman",serif}*{box-sizing:border-box}body{margin:0}main{max-width:1200px;margin:auto;padding:28px 20px 64px}header{border-bottom:3px solid #176b5a;padding-bottom:24px}.adx-home-link{color:#155b4d;font-weight:700;text-decoration:none}.adx-home-link:hover{text-decoration:underline}.adx-home-link:focus-visible,button:focus-visible,input:focus-visible,textarea:focus-visible,select:focus-visible{outline:3px solid #7198ad;outline-offset:3px}.eyebrow{color:#56716a;font-size:.76rem;font-weight:800;letter-spacing:.08em;margin:14px 0 4px}h1,h2,h3{line-height:1.2;margin:.15rem 0}h1{font-size:2.7rem}h2{font-size:1.38rem}h3{font-size:1.1rem}.meta,.context p:last-child,.heading>p{color:#52645f}.context,.workspace,.notice{border-bottom:1px solid #ccd6d0;padding:24px 0}.notice{border-left:4px solid #c68827;padding-left:18px;background:#f9fcfc}.heading{display:flex;justify-content:space-between;gap:24px}.heading>p{margin:0;max-width:360px}.assistant{border-block:1px solid #c4d3cc;margin:24px 0;padding:22px 0}.unavailable{color:#52645f}.pickers{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:16px;margin:18px 0}label{display:grid;gap:6px;color:#24473f;font-size:.92rem;font-weight:750}small{color:#647872;font-size:.8rem;font-weight:500}input,textarea,select{border:1px solid #8ea59d;border-radius:8px;background:#fff;color:#17332f;font:inherit;padding:9px 10px;width:100%}textarea{min-height:72px;resize:vertical}.button,.secondary,.remove{border-radius:8px;cursor:pointer;font:inherit;font-weight:750;padding:9px 14px}.button{border:1px solid #176b5a;background:#176b5a;color:#fff}.button:disabled{background:#9cb6ad;border-color:#9cb6ad;cursor:not-allowed}.secondary{border:1px solid #58847a;background:#fff;color:#174c41}.status{color:#245b4e;min-height:1.5rem}.status.error{color:#982f24}.applied-spec{border-left:3px solid #c68827;color:#53421f;padding-left:10px}.suggestion-list{display:grid;gap:10px;margin-top:14px}.suggestion{display:grid;grid-template-columns:auto 1fr;gap:12px;border:1px solid #b9cbc3;border-radius:8px;background:#fff;padding:14px;cursor:pointer}.suggestion:has(input:checked){border-color:#176b5a;background:#f0f7f3}.suggestion input,.actions input{width:18px;height:18px;accent-color:#176b5a}.suggestion strong,.suggestion small{display:block}.actions{display:flex;align-items:center;gap:12px;flex-wrap:wrap;margin-top:16px}.actions label{display:block}.story-card{border:1px solid #b9cbc3;border-radius:8px;background:#fff;margin:16px 0;padding:18px}.story-head{align-items:center;border-bottom:1px solid #d9e2dd;display:flex;justify-content:space-between;margin-bottom:16px;padding-bottom:12px}.story-number{color:#176b5a}.remove{background:#fff;border:1px solid #b95a4d;color:#8b3026;padding:6px 10px}.story-card label{margin-top:12px}fieldset{border:0;border-top:1px solid #d9e2dd;margin:20px 0 0;padding:16px 0 0}legend{color:#24473f;font-weight:800}@media(max-width:640px){main{padding-inline:16px}.heading{display:block}.pickers{grid-template-columns:1fr}.actions .button{width:100%}}@media(prefers-reduced-motion:reduce){*{scroll-behavior:auto;transition:none!important}}`;

function storyWorkshopController(config) {
  return `(function(){const config=${config},form=document.getElementById('story-workshop-form'),storyList=document.getElementById('story-list'),template=document.getElementById('story-template'),status=document.getElementById('workshop-status');if(!form)return;const number=()=>storyList.querySelectorAll('.story-card').forEach((card,index)=>card.querySelector('.story-number').textContent='Story '+(index+1));const add=(story={})=>{storyList.append(template.content.cloneNode(true));const card=storyList.lastElementChild,scenario=story.scenarios?.[0]||{};for(const[name,value]of Object.entries({title:story.title||'',narrative:story.narrative||'',given:scenario.given||'',when:scenario.when||'',then:scenario.then||''}))card.querySelector('[name='+name+']').value=value;card.querySelector('.remove').onclick=()=>{card.remove();number()};number();return card};document.getElementById('add-story').onclick=()=>add();add();const suggest=document.getElementById('suggest-stories');if(suggest){const suggestionStatus=document.getElementById('suggestion-status'),suggestionList=document.getElementById('suggestion-list'),actions=document.getElementById('suggestion-actions'),accept=document.getElementById('accept-suggestions'),selectAll=document.getElementById('select-all-stories'),marker=document.getElementById('applied-spec');let suggestions=[];const sync=()=>{const boxes=[...suggestionList.querySelectorAll('input')],count=boxes.filter(box=>box.checked).length;selectAll.checked=Boolean(boxes.length)&&count===boxes.length;selectAll.indeterminate=count>0&&count<boxes.length;accept.disabled=!count;accept.textContent=count?'Accept selected suggestions ('+count+')':'Accept selected suggestions'};suggest.onclick=async()=>{suggest.disabled=true;suggest.textContent='Generating...';suggestionStatus.className='status';suggestionStatus.textContent='Preparing a draft. Nothing is being saved.';suggestionList.replaceChildren();actions.hidden=true;marker.hidden=true;try{const response=await fetch(config.storySuggestionsEndpoint,{method:'POST',credentials:'same-origin',headers:{'content-type':'application/json','idempotency-key':crypto.randomUUID()},body:JSON.stringify({model:document.getElementById('story-ai-model').value,templateId:document.getElementById('story-spec-template').value})}),body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.error?.message||'Unable to generate suggestions.');suggestions=body.suggestions||[];const receiptTemplate=body.receipt?.template;marker.textContent=receiptTemplate?'Applied specification: '+receiptTemplate.id+' v'+receiptTemplate.version+' ('+receiptTemplate.digest+')':'Retained context only was used.';marker.hidden=false;suggestions.forEach((story,index)=>{const label=document.createElement('label'),box=document.createElement('input'),copy=document.createElement('span'),title=document.createElement('strong'),detail=document.createElement('small');label.className='suggestion';box.type='checkbox';box.value=index;box.setAttribute('aria-label','Select '+story.title);title.textContent=story.title;detail.textContent=story.narrative;copy.append(title,detail);label.append(box,copy);suggestionList.append(label)});if(suggestions.length){actions.hidden=false;sync();suggestionStatus.textContent=suggestions.length+' suggestions are ready to review.'}else suggestionStatus.textContent='No suggestions were returned. You can continue with manual authoring.'}catch(error){suggestionStatus.className='status error';suggestionStatus.textContent=error.message}finally{suggest.disabled=false;suggest.textContent='Generate suggestions'}};selectAll.onchange=()=>{suggestionList.querySelectorAll('input').forEach(box=>box.checked=selectAll.checked);sync()};suggestionList.onchange=sync;accept.onclick=()=>{const selected=[...suggestionList.querySelectorAll('input:checked')].map(box=>suggestions[box.value]);const cards=[...storyList.querySelectorAll('.story-card')];if(cards.length===1&&![...cards[0].querySelectorAll('[name]')].some(field=>field.value.trim()))cards[0].remove();selected.forEach(add);actions.hidden=true;suggestionStatus.textContent=selected.length+' suggestions added to the editable draft. Review and submit when ready.'}}form.onsubmit=async(event)=>{event.preventDefault();const stories=[...storyList.querySelectorAll('.story-card')].map((card,index)=>({key:'STORY-'+(index+1),title:card.querySelector('[name=title]').value.trim(),narrative:card.querySelector('[name=narrative]').value.trim(),scenarios:[{given:card.querySelector('[name=given]').value.trim(),when:card.querySelector('[name=when]').value.trim(),then:card.querySelector('[name=then]').value.trim()}]}));if(!stories.length||stories.some(story=>!story.title||!story.narrative||!story.scenarios[0].given||!story.scenarios[0].when||!story.scenarios[0].then)){status.className='status error';status.textContent='Each story needs a title, user need, and Given / When / Then example.';return}const button=form.querySelector('[type=submit]');button.disabled=true;button.textContent='Submitting...';try{const response=await fetch(config.storiesEndpoint,{method:'POST',credentials:'same-origin',headers:{'content-type':'application/json','idempotency-key':crypto.randomUUID()},body:JSON.stringify({stories,expectedVersion:config.expectedVersion})}),body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.error?.message||'Unable to submit the story set.');status.textContent='Stories submitted. Opening independent review...';window.setTimeout(()=>window.location.href=config.storyReviewUrl,350)}catch(error){status.className='status error';status.textContent=error.message;button.disabled=false;button.textContent='Submit stories for independent review'}}})()`;
}


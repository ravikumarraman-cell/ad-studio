import { escapeHtml, htmlScriptConfig, shortDigest } from './review-page-utils.mjs'

export function renderStoryReviewPage(changeCase, governance, { canReview, isStoryAuthor, decisionEndpoint, designReviewUrl, releasePlanningUrl, storyWorkshopUrl }) {
  const stories = governance.stories?.stories ?? []
  const approvals = governance.approvals ?? []
  const digest = governance.stories?.storyDigest ?? ''
  const awaitingDecision = changeCase.state === 'AWAITING_STORY_APPROVAL' && Boolean(digest)
  const hasDecisionAccess = awaitingDecision && canReview && !isStoryAuthor
  const nextAction = !awaitingDecision ? changeCase.state === 'DESIGN_REVIEW' ? 'This story contract is approved. Set its release order or continue to design review.' : 'Complete the preceding intake and risk work before opening story review.' : isStoryAuthor ? 'You authored this story contract. An independent reviewer must make the decision.' : hasDecisionAccess ? 'Review the story evidence, then record one independent decision.' : 'This contract is ready for an independent reviewer with permission to decide.'
  const config = htmlScriptConfig({ decisionEndpoint, storyDigest: digest, expectedVersion: changeCase.projectionVersion, designReviewUrl })
  const storyMarkup = stories.map((story, index) => `<article class="story-card"><header><span>Story ${String(index + 1).padStart(2, '0')}</span><h3>${escapeHtml(story.key)} · ${escapeHtml(story.title)}</h3></header><p>${escapeHtml(story.narrative)}</p><div class="scenarios">${(story.scenarios ?? []).map((scenario) => `<section><strong>Given</strong><span>${escapeHtml(scenario.given)}</span><strong>When</strong><span>${escapeHtml(scenario.when)}</span><strong>Then</strong><span>${escapeHtml(scenario.then)}</span></section>`).join('')}</div></article>`).join('') || '<p class="empty">No story revision is ready for review.</p>'
  const decisionPanel = !awaitingDecision
    ? changeCase.state === 'DESIGN_REVIEW' ? `<section class="decision-panel success"><p class="eyebrow">Gate B complete</p><h2>Story contract approved</h2><p>Prioritize the approved stories before continuing through design review.</p><div class="button-row"><a class="button secondary" href="${escapeHtml(releasePlanningUrl)}">Prioritize approved stories</a><a class="button" href="${escapeHtml(designReviewUrl)}">Open design review</a></div></section>` : ''
    : isStoryAuthor ? `<section class="decision-panel notice"><p class="eyebrow">Independent review required</p><h2>Authors cannot approve their own story</h2><p>You can revise the story, but another authorized decision maker must approve or return this exact version.</p><a class="text-link" href="${escapeHtml(storyWorkshopUrl)}">Revise story breakdown</a></section>`
      : hasDecisionAccess ? `<section class="decision-panel decision"><p class="eyebrow">Gate B · Your decision</p><h2>What is your decision?</h2><p>Confirm the user need is clear, the acceptance criteria are observable, and dependencies or risks are visible in the contract.</p><form id="story-decision-form" novalidate><fieldset><legend>Choose one retained outcome</legend><label class="choice approve"><input type="radio" name="decision" value="APPROVED"><span><strong>Approve story</strong><small>Move this Change Case to design review.</small></span></label><label class="choice return"><input type="radio" name="decision" value="REJECTED"><span><strong>Return for revision</strong><small>Send it back to risk and story refinement.</small></span></label></fieldset><label class="rationale" for="story-rationale">Reviewer rationale<textarea id="story-rationale" name="rationale" required aria-describedby="rationale-help" placeholder="State what you verified, or what must be revised."></textarea></label><p id="rationale-help" class="helper">This rationale becomes part of the immutable approval history for <code>${escapeHtml(shortDigest(digest))}</code>.</p><p id="decision-status" class="status" role="status" aria-live="polite"></p><button class="button" type="submit">Record decision</button></form></section>`
        : `<section class="decision-panel notice"><p class="eyebrow">Awaiting review</p><h2>Read access does not advance a gate</h2><p>You can inspect the retained contract. An independent decision maker with workflow permission must record the outcome.</p></section>`
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>ADX Story Review — ${escapeHtml(changeCase.title)}</title><style>
    body{margin:0;background:linear-gradient(145deg,var(--adx-canvas) 0%,#f7faf7 46%,#edf6fa 100%)}main{max-width:1180px;margin:auto;padding:24px clamp(18px,4vw,56px) 64px}.topbar{display:flex;align-items:center;justify-content:space-between;gap:16px;padding-bottom:20px;border-bottom:1px solid var(--adx-line)}.case-state{margin:0;color:var(--adx-muted);font:750 .74rem/1.2 ui-monospace,SFMono-Regular,Menlo,monospace}.hero{display:grid;grid-template-columns:minmax(0,1fr) minmax(240px,.42fr);gap:30px;align-items:end;padding:44px 0 30px}.eyebrow{margin:0 0 9px;color:var(--adx-mint);font-size:.72rem;font-weight:850;letter-spacing:.12em;text-transform:uppercase}.hero h1{max-width:800px;margin:0;font-family:var(--adx-display);font-size:clamp(2.55rem,5vw,4.85rem);font-weight:500;letter-spacing:0;line-height:1}.hero-copy{max-width:690px;margin:16px 0 0;color:var(--adx-copy);font-size:1.06rem;line-height:1.6}.review-compass{align-self:stretch;display:grid;align-content:space-between;gap:26px;padding:20px;border:1px solid #99c5d3;border-radius:var(--adx-radius);background:linear-gradient(150deg,var(--adx-ink),#154a64);box-shadow:var(--adx-shadow);color:#eef9fc}.review-compass span{color:#b9dae5;font-size:.72rem;font-weight:800;letter-spacing:.1em;text-transform:uppercase}.review-compass strong{display:block;max-width:16ch;font-family:var(--adx-display);font-size:1.46rem;font-weight:500;line-height:1.1}.digest{padding-top:14px;border-top:1px solid #4d7890;color:#cce5ef;font:.72rem/1.35 ui-monospace,SFMono-Regular,monospace;overflow-wrap:anywhere}.layout{display:grid;grid-template-columns:minmax(0,1fr) minmax(300px,.43fr);gap:30px;align-items:start}.section-head{display:flex;align-items:end;justify-content:space-between;gap:18px;padding:18px 0 14px;border-bottom:1px solid var(--adx-line)}.section-head h2{margin:0;font-size:1.3rem}.section-head p:not(.eyebrow){max-width:630px;margin:7px 0 0;color:var(--adx-copy);line-height:1.5}.story-count{padding:6px 9px;border-radius:999px;background:var(--adx-mint-soft);color:#075842;font-size:.74rem;font-weight:850;white-space:nowrap}.stories{display:grid;gap:14px;padding-top:16px}.story-card{overflow:hidden;border:1px solid var(--adx-line);border-radius:var(--adx-radius);background:var(--adx-surface);box-shadow:0 5px 15px rgb(16 43 67 / 5%)}.story-card header{padding:17px 19px 13px;border-bottom:1px solid #dce8ee;background:var(--adx-surface-soft)}.story-card header span{color:var(--adx-brand);font-size:.7rem;font-weight:850;letter-spacing:.1em;text-transform:uppercase}.story-card h3{margin:5px 0 0;font-size:1.08rem;line-height:1.3}.story-card>p{margin:0;padding:17px 19px;color:var(--adx-copy);line-height:1.55}.scenarios{display:grid;gap:9px;padding:0 19px 19px}.scenarios section{display:grid;grid-template-columns:54px 1fr;gap:5px 12px;padding:12px;border-left:3px solid var(--adx-brand);background:#f7fbfd}.scenarios strong{font-size:.76rem;text-transform:uppercase;letter-spacing:.05em;color:var(--adx-brand-deep)}.scenarios span{color:var(--adx-copy);font-size:.91rem;line-height:1.4}.decision-panel{position:sticky;top:18px;padding:21px;border:1px solid var(--adx-line);border-radius:var(--adx-radius);background:var(--adx-surface);box-shadow:var(--adx-shadow)}.decision-panel h2{margin:0;font-size:1.38rem;line-height:1.2}.decision-panel>p:not(.eyebrow){color:var(--adx-copy);line-height:1.55}.decision-panel.decision{border:2px solid var(--adx-brand)}.decision-panel.notice{background:#fffaf0;border-color:#e6c67a}.decision-panel.success{background:#f0fbf5;border-color:#8fcdae}.choice{display:flex;gap:11px;align-items:flex-start;margin:10px 0;padding:14px;border:1px solid var(--adx-line);border-radius:8px;cursor:pointer}.choice:has(input:checked){border:2px solid var(--adx-brand);background:#edf8fd}.choice.return:has(input:checked){border-color:var(--adx-gold);background:#fff8e7}.choice input{margin-top:4px;accent-color:var(--adx-brand)}.choice strong,.choice small{display:block}.choice small{margin-top:3px;color:var(--adx-muted)}fieldset{margin:20px 0;border:0;padding:0}legend{font-size:.86rem;font-weight:800}.rationale{display:grid;gap:7px;font-size:.86rem;font-weight:800}.rationale textarea{min-height:112px;resize:vertical;border:1px solid #7999ab;border-radius:8px;padding:10px;color:var(--adx-ink);font:400 .94rem/1.5 var(--adx-body)}.helper{color:var(--adx-muted);font-size:.79rem;line-height:1.45}.status{min-height:1.4em;font-weight:750}.status.error{color:var(--adx-danger)}.button{display:inline-block;border:0;border-radius:8px;padding:11px 14px;background:var(--adx-brand);color:#fff;font:800 .88rem var(--adx-body);text-decoration:none;cursor:pointer}.button:hover{background:var(--adx-brand-deep)}.button.secondary{background:var(--adx-surface-soft);color:var(--adx-brand-deep);border:1px solid #a7c3d0}.button-row{display:flex;flex-wrap:wrap;gap:9px;margin-top:16px}.text-link{color:var(--adx-brand-deep);font-weight:800}.empty{padding:22px;color:var(--adx-muted);background:var(--adx-surface);border:1px dashed var(--adx-line)}@media(max-width:800px){main{padding-bottom:40px}.hero,.layout{grid-template-columns:1fr}.review-compass{min-height:170px}.decision-panel{position:static}.section-head{align-items:start;flex-direction:column}}@media(max-width:520px){.topbar{align-items:flex-start;flex-direction:column}.hero{padding:30px 0 22px}.hero h1{font-size:2.55rem}.scenarios section{grid-template-columns:1fr}.story-count{white-space:normal}}
  </style></head><body><main><header class="topbar"><div class="adx-page-brand"><b>ADX</b><span>Delivery control</span></div><p class="case-state">${escapeHtml(changeCase.state)} · Version ${escapeHtml(changeCase.projectionVersion)}</p></header><section class="hero"><div><p class="eyebrow">Gate B · Independent story approval</p><h1>${escapeHtml(changeCase.title)}</h1><p class="hero-copy">A focused review of the user-value contract. The decision you record is bound to this exact story revision and cannot be inferred from a page view.</p></div><aside class="review-compass" aria-label="Review context"><span>Decision compass</span><strong>${escapeHtml(nextAction)}</strong><code class="digest" title="${escapeHtml(digest)}">${escapeHtml(shortDigest(digest))}</code></aside></section><section class="layout"><section><header class="section-head"><div><p class="eyebrow">Retained story evidence</p><h2>Reviewable user outcomes</h2><p>Each story includes an observable Given / When / Then contract for independent assessment.</p></div><span class="story-count">${stories.length} ${stories.length === 1 ? 'STORY' : 'STORIES'}</span></header><div class="stories">${storyMarkup}</div></section><aside>${decisionPanel}</aside></section></main><script>const config=${config};const form=document.getElementById('story-decision-form');form?.addEventListener('submit',async(event)=>{event.preventDefault();const status=document.getElementById('decision-status');const decision=new FormData(form).get('decision');const rationale=document.getElementById('story-rationale').value.trim();status.className='status';if(!decision||!rationale){status.textContent='Choose a decision and enter a rationale before recording it.';status.classList.add('error');return}const button=form.querySelector('button');button.disabled=true;button.textContent='Recording...';try{const response=await fetch(config.decisionEndpoint,{method:'POST',credentials:'same-origin',headers:{'content-type':'application/json','idempotency-key':crypto.randomUUID()},body:JSON.stringify({storyDigest:config.storyDigest,decision,rationale,expectedVersion:config.expectedVersion})});const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.error?.message||body.code||'Unable to record the decision.');status.textContent=decision==='APPROVED'?'Story approved. Opening design review...':'Story returned for revision. Refreshing the review...';window.setTimeout(()=>{window.location.href=decision==='APPROVED'?config.designReviewUrl:window.location.href},500)}catch(error){status.textContent=error.message;status.classList.add('error');button.disabled=false;button.textContent='Record decision'}});</script></body></html>`
}

function escapeHtml(value) {
  return String(value ?? '').replace(/[&<>"']/g, (character) => ({
    '&': '&amp;',
    '<': '&lt;',
    '>': '&gt;',
    '"': '&quot;',
    "'": '&#39;',
  }[character]));
}

export function renderExecutionHandoffPage(changeCase, options) {
  const {
    canSubmit,
    submitReason = null,
    signedInRoles = [],
    dispatchEndpoint,
    statusEndpoint,
    evidenceReviewUrl,
    candidateUrl,
    providers = [],
    templates = [],
  } = options;

  const ready = changeCase.state === 'READY_FOR_EXECUTION';
  const enabled = providers.filter((provider) => provider.enabled);
  const submitBlockedReason = !ready
    ? null
    : canSubmit && enabled.length
      ? null
      : !canSubmit
        ? submitReason || 'You do not have permission to submit this implementation run.'
        : 'No enabled implementation providers are configured for this server.';

  const providerChoices = enabled
    .map(
      (provider, index) =>
        `<label class="runner-choice"><input type="radio" name="provider" value="${escapeHtml(provider.id)}"${index ? '' : ' checked'}><span><strong>${escapeHtml(provider.label)}</strong><small>${escapeHtml(provider.description)}</small></span></label>`,
    )
    .join('');

  const templateChoices = templates
    .map(
      (template, index) =>
        `<option value="${escapeHtml(template.id)}" title="${escapeHtml(template.guidance ?? '')}"${index ? '' : ' selected'}>${escapeHtml(template.label)} - ${escapeHtml(template.description)} ${escapeHtml(template.guidance ?? '')}</option>`,
    )
    .join('');

  const blockedNotice = submitBlockedReason
    ? `<p class="field-help error">Submission disabled: ${escapeHtml(submitBlockedReason)}</p>`
    : '';

  const readyView = `<section class="request-panel"><p class="eyebrow">BOUNDED IMPLEMENTATION</p><h2>Start a controlled implementation run</h2><p>ADX issues a signed lease, creates a disposable workspace, requests a constrained patch, validates it, and retains a candidate only after the checks pass.</p><form id="dispatch-form"><fieldset><legend>Implementation runner</legend><p class="field-help">Story decomposition models are configured separately and cannot run or approve implementation.</p>${providerChoices}</fieldset><label class="select-label">Implementation specification<select id="coding-spec-template">${templateChoices}</select><small>A reviewed specification is bound to the signed task. It cannot expand the lease scope.</small></label><label class="confirm"><input id="confirmation" type="checkbox"> I understand this run can modify only its disposable candidate workspace. A successful run is not approval or delivery.</label><button id="submit" class="button primary" type="submit" disabled>Run bounded implementation</button><p id="status" class="status" role="status" aria-live="polite"></p>${blockedNotice}</form></section><section id="progress-console" class="run-console" hidden aria-live="polite"><header class="console-header"><div><p class="eyebrow">LIVE BOUNDED RUN</p><h2>Implementation activity</h2><p id="run-headline">Recording your request...</p></div><p id="run-clock" class="run-clock">00:00</p></header><ol class="run-steps"><li data-stage="leased"><span>1</span><div><strong>Lease issued</strong><small>Scope and limits are signed.</small></div></li><li data-stage="started"><span>2</span><div><strong>Sandbox working</strong><small>Preparing a bounded patch.</small></div></li><li data-stage="validated"><span>3</span><div><strong>Candidate validated</strong><small>Fixed tests determine promotion.</small></div></li><li data-stage="verified"><span>4</span><div><strong>Ready for verification</strong><small>A code preview becomes available only after the candidate passes validation.</small></div></li></ol><section class="engagement-note"><strong>What ADX can show safely</strong><p>Live status reflects durable run events instead of untrusted console output. The code preview becomes available only after the candidate passes validation, so incomplete or untrusted model output is never presented as generated code.</p></section><ul id="progress-events" class="event-log"></ul><section id="failure-details" class="failure-details" hidden><h3>Run stopped</h3><p id="failure-message"></p><dl><dt>Diagnostic code</dt><dd id="failure-code"></dd><dt>Provider stage</dt><dd id="failure-provider-stage"></dd><dt>Validation hint</dt><dd id="failure-validation-hint"></dd></dl><p id="failure-reason" hidden></p><p id="failure-validation-excerpt" hidden></p><p class="candidate-link"><a id="generated-candidate-link" href="${escapeHtml(candidateUrl ?? '')}">View generated candidate</a><a id="verification-link" href="${escapeHtml(evidenceReviewUrl ?? '')}" hidden>Open independent verification</a></p></section></section><script>const submitBlocked=false;const form=document.getElementById('dispatch-form'),button=document.getElementById('submit'),status=document.getElementById('status'),confirmation=document.getElementById('confirmation'),providers=[...document.querySelectorAll('input[name="provider"]')];if(form&&button&&status){const syncControls=()=>{const providerSelected=providers.some((input)=>input.checked);button.disabled=submitBlocked||!providerSelected||!confirmation?.checked};confirmation?.addEventListener('change',syncControls);providers.forEach((input)=>input.addEventListener('change',syncControls));syncControls();form.addEventListener('submit',async (event)=>{event.preventDefault();button.disabled=true;status.textContent='Requesting a signed lease… this can take a moment.';button.classList.add('is-busy');try{const response=await fetch(${JSON.stringify(dispatchEndpoint)},{method:'POST',headers:{'content-type':'application/json','idempotency-key':crypto.randomUUID()},body:JSON.stringify({provider:document.querySelector('input[name="provider"]:checked')?.value,templateId:document.getElementById('coding-spec-template')?.value,expectedVersion:${JSON.stringify(changeCase.projectionVersion)}})});const body=await response.json().catch(()=>null);if(!response.ok)throw new Error(body?.error?.message||body?.code||'Implementation request failed.');status.textContent='Run accepted. Showing recorded activity...';}catch(error){status.className='status error';status.textContent=error.message;button.disabled=false;button.classList.remove('is-busy');}});}</script><script>const config=${JSON.stringify({ dispatchEndpoint, statusEndpoint, evidenceReviewUrl, candidateUrl, expectedVersion: changeCase.projectionVersion }).replace(/</g, '\u003c')};const stageOrder=['leased','started','validated','verified'];let pollTimer=null,clockTimer=null,runStartedAt=null,lastEventKey='';const byId=id=>document.getElementById(id);const eventLabels={AgentRunLeased:{title:'Lease issued',detail:'Signed scope and policy limits have been recorded.',stage:'leased'},AgentRunStarted:{title:'Building the bounded Health-X candidate',detail:'ADX is preparing the disposable workspace, requesting the constrained patch, then will run the fixed Health-X verification.',stage:'started'},AgentRunCompleted:{title:'Candidate validated',detail:'The run completed and the candidate is ready for independent verification.',stage:'verified'},AgentRunFailed:{title:'Runner stopped',detail:'The candidate was not promoted.',stage:'started',failed:true},AgentRunQuotaExceeded:{title:'Runner limit reached',detail:'The bounded run reached a configured limit.',stage:'started',failed:true},AgentRunCancellationObserved:{title:'Run cancelled',detail:'The run was cancelled before promotion.',stage:'started',failed:true},AgentRunLeaseRevoked:{title:'Lease revoked',detail:'The execution lease was revoked.',stage:'leased',failed:true}};function clock(){if(!runStartedAt)return;const seconds=Math.max(0,Math.floor((Date.now()-runStartedAt)/1000));byId('run-clock').textContent=String(Math.floor(seconds/60)).padStart(2,'0')+':'+String(seconds%60).padStart(2,'0')}function stopLiveTimers(){if(pollTimer){clearInterval(pollTimer);pollTimer=null}if(clockTimer){clearInterval(clockTimer);clockTimer=null}}function eventTime(value){try{return new Date(value).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit',second:'2-digit'})}catch{return '--:--:--'}}function setStages(stage,failed){const active=stageOrder.indexOf(stage);document.querySelectorAll('.run-steps li').forEach((item,index)=>{item.classList.toggle('done',!failed&&index<active);item.classList.toggle('active',!failed&&index===active);item.classList.toggle('failed',Boolean(failed)&&index===active)})}function renderEvents(events){const log=byId('progress-events');if(!log)return;log.innerHTML=events.map(event=>{const type=String(event.eventType||'').replace(/\.v\d+$/,'');const info=eventLabels[type]||{title:event.eventType,detail:'Durably recorded execution event.',stage:'started'};return '<li><time>'+eventTime(event.occurredAt)+'</time><div><strong>'+info.title+'</strong><span>'+info.detail+'</span></div></li>'}).join('')}function showFailure(event){const panel=byId('failure-details');if(!panel)return;const details=event.errorDetails||{},code=event.errorCode||'CODING_AGENT_EXECUTION_FAILED',timings=event.timings||{};const next=details.providerStatus===429?'Wait briefly, then retry.':details.responseIssue?'The provider returned an unusable patch response. Retry once; contact the provider administrator if it repeats.':'Inspect the configured runner and validation command.';panel.hidden=false;panel.innerHTML='<h3>The bounded runner stopped before producing a candidate.</h3><p>ADX retained this failure. It did not promote a candidate.</p><dl><dt>Diagnostic code</dt><dd>'+code+'</dd><dt>Next action</dt><dd>'+next+'</dd>'+(details.validationOutputExcerpt?'<dt>Validation output</dt><dd><pre class="failure-output" style="white-space:pre-wrap;max-height:14rem;overflow:auto;margin:.5rem 0 0;padding:.75rem;border:1px solid #d9c5bd;background:#fff7f2;font:inherit">'+escapeHtml(details.validationOutputExcerpt)+'</pre></dd>':'')+(details.validationFailureReason?'<dt>Fallback reason</dt><dd>'+escapeHtml(details.validationFailureReason)+'</dd>':'')+(details.providerStatus?'<dt>Provider status</dt><dd>HTTP '+details.providerStatus+'</dd>':'')+(details.providerRequestId?'<dt>Request ID</dt><dd>'+details.providerRequestId+'</dd>':'')+(timings.totalMs?'<dt>Total runtime</dt><dd>'+(timings.totalMs/1000).toFixed(1)+' seconds</dd>':'')+'</dl>'}async function poll(runId){try{const response=await fetch(config.statusEndpoint);if(!response.ok)throw new Error('Unable to read run status.');const data=await response.json(),events=(data.events||[]).filter(event=>event.runId===runId);renderEvents(events);const latest=events.at(-1);if(!latest)return;const type=String(latest.eventType||'').replace(/\.v\d+$/,'');const info=eventLabels[type]||{title:'Implementation activity',detail:'Run state updated.',stage:'started'};byId('run-headline').textContent=info.title+' - '+info.detail;setStages(info.stage,info.failed);const key=latest.runId+':'+latest.sequence;if(key!==lastEventKey){lastEventKey=key;clock()}if(['AgentRunCompleted','AgentRunFailed','AgentRunQuotaExceeded','AgentRunCancellationObserved','AgentRunLeaseRevoked'].includes(type)){stopLiveTimers();if(type==='AgentRunCompleted'){byId('candidate-link').hidden=false;byId('verification-link').hidden=false}else showFailure(latest)}}catch(error){byId('run-headline').textContent=error.message}}function beginRun(runId){stopLiveTimers();runStartedAt=Date.now();lastEventKey='';byId('progress-console').hidden=false;byId('run-headline').textContent='Lease issued - recording bounded execution.';setStages('leased',false);clock();poll(runId);pollTimer=setInterval(()=>poll(runId),1500);clockTimer=setInterval(clock,1000);byId('confirmation')?.addEventListener('change',event=>{byId('submit').disabled=submitBlocked||!event.currentTarget.checked});}
</script></body></html>`;
  const headStyles = `<style>body{margin:0;background:#eef4f1;color:#142b31;font:16px/1.5 "Avenir Next","Segoe UI",sans-serif;overflow-x:hidden}main{max-width:1120px;margin:0 auto;padding:30px clamp(18px,4vw,56px) 64px;min-width:0}.topbar{display:flex;justify-content:space-between;align-items:center;gap:18px;padding-bottom:20px;border-bottom:1px solid #bdd0c6}.brand{display:flex;gap:10px;align-items:center;font-size:.78rem;font-weight:800;letter-spacing:.09em;text-transform:uppercase}.brand b{display:grid;place-items:center;width:34px;height:26px;border-radius:5px;background:#153b3c;color:#f1f7f4;font-size:.68rem}.state{margin:0;color:#567074;font:800 .74rem ui-monospace,SFMono-Regular,monospace}.hero{display:grid;grid-template-columns:minmax(0,1fr) minmax(230px,.34fr);gap:28px;align-items:end;padding:44px 0 30px}.eyebrow{margin:0 0 7px;color:#28705d;font-size:.72rem;font-weight:800;letter-spacing:.1em;text-transform:uppercase}.hero h1{margin:0;max-width:760px;font-family:Georgia,"Times New Roman",serif;font-size:clamp(2.3rem,5vw,4.25rem);font-weight:500;line-height:1.02;overflow-wrap:anywhere}.hero-copy{max-width:660px;margin:14px 0 0;color:#577075;font-size:1.05rem}.assurance{padding:17px;border:1px solid #b8cdbf;border-left:5px solid #26705d;background:#fbfdfb}.assurance strong,.assurance span{display:block}.assurance span{color:#607a7b;font-size:.85rem}.request-panel,.notice{padding:24px;border:1px solid #c4d4ca;background:#fbfdfb;min-width:0}.request-panel{border-top:4px solid #28705d}.request-panel h2,.run-console h2,.notice h2{margin:0;font-family:Georgia,"Times New Roman",serif;font-size:1.7rem;font-weight:500;line-height:1.15}.request-panel>p,.notice p{color:#587176}.request-panel form{display:grid;gap:17px;margin-top:20px;min-width:0}.request-panel fieldset{margin:0;padding:0;border:0;min-width:0}.request-panel legend{font-weight:800}.field-help{margin:5px 0 9px;color:#607a7b;font-size:.87rem;overflow-wrap:anywhere}.runner-choice{display:flex;gap:10px;margin:8px 0;padding:11px;border:1px solid #d2ded7;background:#fff;cursor:pointer;min-width:0}.runner-choice input{margin-top:2px}.runner-choice strong{display:block}.runner-choice small{display:block;color:#607a7b;overflow-wrap:anywhere}.select-label{display:grid;gap:6px;font-weight:800;min-width:0}.select-label select{width:100%;max-width:100%;min-width:0;min-height:2.45rem;padding:8px 12px;box-sizing:border-box}.confirm{display:flex;gap:10px;align-items:flex-start;min-width:0}.confirm input{margin-top:.25rem}.button{appearance:none;border:1px solid #2c6f5c;background:#2c6f5c;color:#fff;border-radius:8px;padding:10px 16px;font-weight:800;cursor:pointer}.button:disabled{opacity:.45;cursor:not-allowed}.status{margin:0;font-size:.92rem;color:#607a7b}.status.error{color:#a9443a}.run-console{margin-top:20px;padding:24px;border:1px solid #c4d4ca;background:#fbfdfb}.console-header{display:flex;justify-content:space-between;gap:16px;align-items:flex-start;padding-bottom:14px;border-bottom:1px solid #dce8dd}.run-clock{margin:0;font:800 1rem ui-monospace,SFMono-Regular,monospace;color:#28705d}.run-steps,.event-log{padding-left:18px}.event-log{display:grid;gap:10px;margin:18px 0 0}.failure-details{margin-top:18px;padding:16px;border:1px solid #f0c9bf;background:#fff7f4}.failure-details h3{margin:0 0 8px}.candidate-link{display:flex;flex-wrap:wrap;gap:12px;margin-top:16px}.busy-indicator{display:none;width:1em;height:1em;border:2px solid currentColor;border-right-color:transparent;border-radius:50%;animation:busy-spin .7s linear infinite}.is-busy .busy-indicator{display:inline-block}.button.is-busy{display:inline-flex;align-items:center;gap:.55em}.sr-only{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0}@keyframes busy-spin{to{transform:rotate(360deg)}}@media (prefers-reduced-motion:reduce){.busy-indicator{animation:none}}@media (max-width:800px){.hero{grid-template-columns:1fr}.console-header{flex-direction:column}}</style>`;
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>ADX Implementation - ${escapeHtml(changeCase.title)}</title>${headStyles}</head><body><main><header class="topbar"><div class="brand"><b>ADX</b><span>Delivery control</span></div><p class="state">${escapeHtml(changeCase.state)} · Version ${escapeHtml(changeCase.projectionVersion)}</p></header><section class="hero"><div><p class="eyebrow">Between Gate C and Gate D</p><h1>${escapeHtml(changeCase.title)}</h1><p class="hero-copy">A live, bounded implementation run. ADX retains facts about the run and only opens verification after a candidate has passed its fixed validation.</p></div><aside class="assurance"><strong>Controlled execution</strong><span>Signed lease · Disposable workspace · Fixed validation</span></aside></section>${readyView}</main></body></html>`;
}
